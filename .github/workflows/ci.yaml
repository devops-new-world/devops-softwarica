Name: Develop My Website

on:
    push:
        branches:
          -"**"
jobs:
    docker-build:
      runs-on:ubuntu-latest
    deploys:
          runs-on: ubuntu-latest
          steps:
             -name:checkout coode
              uses: actions/checkout@v4
                       

