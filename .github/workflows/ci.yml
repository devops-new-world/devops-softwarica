Name: Develop My Website

on:
    push:
        branches:
          -"**"
jobs:
    docker-build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: Build Docker Image
              run: |
                docker build -t my-website:latest .    
    deploys:
            runs-on: ubuntu-latest
            steps:
                - name:checkout coode
                  uses: actions/checkout@v4


                