name: Deploy Rohan Website

on:
  push:
    branches:
      - "rohan"
      - "feature/rohan"
  workflow_dispatch:

jobs:
  docker-build:
    name: Build rohan Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Check Docker availability
        run: docker --version

      - name: Build Docker image
        run: |
          docker build -t rohan -f sites/rohan/Dockerfile sites/rohan

  deploy:
    name: Deploy rohan website
    runs-on: ubuntu-latest
    needs: docker-build
    env:
      SSH_HOST: ${{ secrets.HOST }}
      SSH_USER: ${{ secrets.USERNAME }}
      SSH_KEY: ${{ secrets.KEY }}
      SSH_PASSWORD: ${{ secrets.PASSWORD }}
      DEPLOY_PATH: /var/www/html/rohan
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Validate required secrets
        run: |
          if [ -z "$SSH_HOST" ] || [ -z "$SSH_USER" ]; then
            echo "Missing required secrets: HOST and/or USERNAME"
            exit 1
          fi

          if [ -z "$SSH_KEY" ] && [ -z "$SSH_PASSWORD" ]; then
            echo "Provide at least one authentication secret: KEY (preferred) or PASSWORD"
            exit 1
          fi

      - name: Set up SSH
        if: ${{ env.SSH_KEY != '' }}
        run: |
          install -m 700 -d ~/.ssh
          KEY_CONTENT="$SSH_KEY"
          KEY_CONTENT="${KEY_CONTENT#\"}"
          KEY_CONTENT="${KEY_CONTENT%\"}"

          if echo "$KEY_CONTENT" | grep -q '\\n'; then
            printf '%b' "$KEY_CONTENT" | tr -d '\r' > ~/.ssh/id_rsa
          else
            printf '%s\n' "$KEY_CONTENT" | tr -d '\r' > ~/.ssh/id_rsa
          fi

          if ! grep -q "BEGIN .*PRIVATE KEY" ~/.ssh/id_rsa 2>/dev/null; then
            printf '%s' "$KEY_CONTENT" | base64 -d > ~/.ssh/id_rsa 2>/dev/null || true
          fi

          chmod 600 ~/.ssh/id_rsa

          if ! ssh-keygen -y -f ~/.ssh/id_rsa >/dev/null 2>&1; then
            echo "Invalid SSH key in secret KEY. Save the full private key (including BEGIN/END lines) or a valid base64-encoded private key."
            exit 1
          fi

          ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Copy files via SSH
        run: |
          SCP_OPTS="-o StrictHostKeyChecking=accept-new -o PubkeyAcceptedAlgorithms=+ssh-rsa -o HostkeyAlgorithms=+ssh-rsa"

          if [ -n "$SSH_KEY" ]; then
            scp -i ~/.ssh/id_rsa -o IdentitiesOnly=yes -o PreferredAuthentications=publickey $SCP_OPTS sites/rohan/index.html "$SSH_USER@$SSH_HOST:$DEPLOY_PATH" && exit 0
            echo "Key-based auth failed, attempting password fallback if PASSWORD secret is set..."
          fi

          if [ -n "$SSH_PASSWORD" ]; then
            sudo apt-get update -y
            sudo apt-get install -y sshpass
            sshpass -p "$SSH_PASSWORD" scp $SCP_OPTS -o PreferredAuthentications=password -o PubkeyAuthentication=no sites/rohan/index.html "$SSH_USER@$SSH_HOST:$DEPLOY_PATH" && exit 0
          fi

          echo "Deployment failed: authentication unsuccessful. Verify KEY matches USERNAME@HOST, or set PASSWORD secret."
          exit 1
