version: "3.8"
services:
  mongo:
    image: mongo:7
    restart: always
    volumes:
      - mongo_data:/data/db

  backend:
    image: rezenn/rijen-backend:latest
    restart: always
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://rezenkhadgi_db_user:cO7jYybmUX8oZmuw@cluster0.61adj2k.mongodb.net/
    depends_on:
      - mongo

  frontend:
    image: rezenn/rijen-frontend:latest
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  mongo_data:
