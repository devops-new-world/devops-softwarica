Name: Deploy my website

on:
  push:
    branches:
      -"feature/bhusan"
jobs:
  docker-build:
    runs-on:ubuntu-latest
    steps:
    - name:checkout code
      uses:actions/checkout@v4
    - name: build docker image
      

    
        